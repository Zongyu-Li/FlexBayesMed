% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qctpt.R
\name{qctpt}
\alias{qctpt}
\title{Quantile Function of the Mean-Adjusted Two-Piece Student's t-Distribution (CTPT)}
\usage{
qctpt(p, gamma, nu, lower.tail = TRUE, log.p = FALSE)
}
\arguments{
\item{p}{Numeric vector of probabilities in \eqn{[0,1]}.}

\item{gamma}{Positive scalar (\eqn{\gamma > 0}). The skewness parameter.}

\item{nu}{Either a numeric \eqn{>1} (degrees of freedom) or the string \code{"normal"}.}

\item{lower.tail}{Logical; if \code{TRUE} (default), probabilities are \eqn{P(X \le q)}.}

\item{log.p}{Logical; if \code{TRUE}, \code{p} is treated as \eqn{\log(p)}.}
}
\value{
Numeric vector of quantiles corresponding to the given probabilities.
}
\description{
Inverts the CDF of the CTPT distribution to find \eqn{\varepsilon} such that
\eqn{P(X \le \varepsilon) = p}.
}
\details{
The distribution is defined by a piecewise CDF:
\deqn{
  F(\varepsilon|\gamma,\nu) =
  \begin{cases}
    \displaystyle
    \frac{2}{1 + \gamma^2}\,F_\nu\bigl[\gamma\,(\varepsilon + m)\bigr], & \varepsilon < -m,\\[2em]
    \displaystyle
    \frac{2\,\gamma^2}{1 + \gamma^2}\,F_\nu\!\Bigl(\tfrac{\varepsilon + m}{\gamma}\Bigr)
      \;+\;\frac{1 - \gamma^2}{1 + \gamma^2}, & \varepsilon \ge -m,
  \end{cases}
}
where \eqn{m = m(\gamma,\nu)} is the mean-adjustment term, and \eqn{F_\nu} is the
standard Student's t (if \eqn{\nu > 1}) or standard normal (if \code{nu="normal"}) CDF.
}
\examples{
qctpt(p = 0.975, gamma = 1, nu = "normal") # Returns 1.96
qctpt(p = 0.025, gamma = 2, nu = 2) # Returns -3.40

}
\seealso{
\code{\link{pctpt}} for the CDF and \code{\link{dctpt}} for the PDF.
}
