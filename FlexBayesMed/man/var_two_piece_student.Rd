% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/var_two_piece_student.R
\name{var_two_piece_student}
\alias{var_two_piece_student}
\title{Variance of the Uncentered Two-Piece Student \emph{t} (or Normal) Distribution}
\usage{
var_two_piece_student(gamma, nu)
}
\arguments{
\item{gamma}{A positive numeric value giving the skewness parameter (must be \eqn{\gamma > 0}).}

\item{nu}{Either:
\itemize{
\item A numeric value \code{> 2}, giving the degrees of freedom \eqn{\nu} for the Student \emph{t}
(required for a finite variance).
\item The character string \code{"normal"} to indicate the limiting two-piece normal case
(i.e., \eqn{\nu = \infty}).
}}
}
\value{
A numeric value giving the variance of the uncentred two-piece distribution.
}
\description{
This function calculates the variance of the uncentred two-piece Student \emph{t} distribution
(see Proposition 2 in ...) for \eqn{\gamma > 0} and \eqn{\nu > 2}.
It also handles the limiting "normal" case (i.e., \eqn{\nu \to \infty}).
}
\details{
When \code{nu} is a finite numeric value \eqn{> 2}, the variance formula is:
\deqn{
  \mathrm{Var}(\varepsilon \mid \gamma,\nu)
  =
    \frac{\nu}{\nu - 2}
    \Bigl(\gamma^2 - 1 + \tfrac{1}{\gamma^2}\Bigr)
    \;-\;
    m(\gamma,\nu)^2,
}
where
\deqn{
  m(\gamma,\nu)
  =
    \frac{2\,\nu\,\Gamma\bigl((\nu + 1)/2\bigr)}{\sqrt{\pi\,\nu}\,(\nu-1)\,\Gamma\bigl(\nu/2\bigr)}
    \,\bigl(\gamma - \tfrac{1}{\gamma}\bigr).
}

When \code{nu = "normal"}, the variance is that of the uncentred two-piece normal distribution:
\deqn{
  \mathrm{Var}(\varepsilon \mid \gamma,\nu=\infty)
  =
    \bigl(\gamma^2 - 1 + \tfrac{1}{\gamma^2}\bigr)
    \;-\;
    \Bigl(\sqrt{\tfrac{2}{\pi}}\bigl(\gamma - \tfrac{1}{\gamma}\bigr)\Bigr)^2.
}
}
\examples{
# Variance for gamma=2, nu=5
var_two_piece_student(2, 5)

# Variance in the limiting normal case for gamma=2
var_two_piece_student(2, "normal")

}
